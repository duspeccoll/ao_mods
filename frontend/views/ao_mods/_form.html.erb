<%= form_tag({:action => :batch}, {:class => "form-horizontal", :id => "mods_download_form"}) do |form| %>
  <div class="form-group">
    <div class="controls col-sm-4">
      <textarea rows="10" id="mods_downloads" class="form-control" name="mods_downloads"></textarea>
    </div>
  </div>

  <div class="form-group">
    <div class="controls">
      <%= submit_tag I18n.t("plugins.ao_mods.actions.download"), :class => "btn btn-primary" %>
    </div>
  </div>
<% end %>
